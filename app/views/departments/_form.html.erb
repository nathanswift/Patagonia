<h1><%= @department.id ? 'Edit Sub' : 'Create Sub' %></h1>
<hr />
<%= form_for @department do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, required: true, autofocus: true %>
<% end %>
<%# <%= select_tag, @departments = option_for_select(@departments.map{|department| ["#{department.names} #{department.wonames} #{department.kids} #{department.cats}", department.id] }) %> 


